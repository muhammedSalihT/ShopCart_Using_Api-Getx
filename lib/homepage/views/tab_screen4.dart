import 'package:flutter/material.dart';

class TabScreen4 extends StatelessWidget {
  const TabScreen4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          height: 50,
          color: Colors.orange,
        )
      ],
    );
  }
}